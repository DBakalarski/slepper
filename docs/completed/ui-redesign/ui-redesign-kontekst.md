# UI Redesign — kontekst

**Branch:** `feature/ui-redesign`
**Ostatnia aktualizacja:** 2026-05-28 (po Fazie 6 — autopilot finalize)

## Postęp

> **2026-05-28 (autopilot finalize):** Faza 6 ukończona, Faza 7 manual test deferred do usera. Walidacja CLI (`tsc --noEmit` + `expo lint`) PASS dla wszystkich faz 0-6. Manual test master checklist wygenerowany — patrz [manual-test-master.md](./manual-test-master.md). Autopilot przechodzi do walidacji końcowej + `/dev-docs-complete` + `/dev-compound`.


- ✅ **Faza 0 — Design system foundation** (2026-05-28): tokeny tailwind (kolory purple-light/purple-soft/success/success-soft/orange-soft/text-muted, radii card/pill, shadow card, fontFamily display/mono, `darkMode: 'class'`), 9 primitives (`Avatar/Card/Badge/IconButton/ProgressBar/ProgressBarStacked/ProgressRing/SegmentedControl/Switch`), helper `sleep-norms.ts` (WHO+AAP hybrid). `react-native-svg@15.15.5` dociągnięty TRANZYTYWNIE via `lucide-react-native@1.17.0` — brak osobnej instalacji. `expo-linear-gradient` SKIPPED.
- ✅ **Code review Fazy 0** (2026-05-28): severity gate **CZYSTE** — 0 P1, 0 P2, 4 P3 (opcjonalne nity do rozważenia w Fazie 6). Raport: `review-faza-0.md`. Manual test checklist: `manual-test-faza-0.md` (non-blocking, wykonanie on-device dopiero po Fazie 2/3 gdy primitives wejdą na ekran). Walidacja: `tsc --noEmit` PASS, `expo lint` PASS, `npm ls` potwierdza `lucide-react-native@1.17.0` + transitywny `react-native-svg@15.15.5`.
  - **Kluczowe wnioski**: (1) `getNormForChild` jest pure i idealnym kandydatem na pierwszy unit test gdy projekt dostanie test runner; (2) HEX literals w `ProgressRing`/`Switch` duplikują tailwind tokens — uzasadnione (RN API), ale do wyciągnięcia do `src/lib/colors.ts` przy 3+ duplikacji; (3) `SegmentedControl` używa Reanimated `withTiming` 200ms zgodnie z decyzją (NIE withSpring — zacinanie na Android); (4) wszystkie primitives mają WCAG-friendly accessibility props (role + label + value gdzie pasuje).
- ✅ **Faza 1 — Dark mode manual override** (2026-05-28): `useThemeStore` (Zustand + AsyncStorage persist, key `theme-mode`, state `mode: 'system'|'light'|'dark'`, default `'system'`) + `ThemeProvider` (czyta `useColorScheme()`, oblicza `effectiveTheme`, opakowuje children w root `<View>` z klasą `dark` gdy potrzebne). Mount w `src/app/_layout.tsx` — `ThemeProvider` powyżej `RootLayoutContent` (Stack + StatusBar). `StatusBar.style` bindowany do `effectiveTheme` (`'light'` w dark, `'dark'` w light). **UI bottom sheet w Fazie 5** — store gotowy do konsumpcji. Walidacja: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe decyzje**: (1) `useEffectiveTheme()` jako exportowany hook z `ThemeProvider.tsx` — będzie konsumowany przez Profil (Faza 5) i potencjalnie inne komponenty wymagające bezpośredniego dostępu do `'light'|'dark'` (np. wybór koloru SVG); (2) `RootLayoutContent` wyciągnięte jako sub-component — `useEffectiveTheme()` musi być wywołane WEWNĄTRZ `<ThemeProvider>`, więc nie da się tego zrobić w `RootLayout` bezpośrednio; (3) fallback `systemScheme === null` → `'light'` (RN może zwrócić null przed inicjalizacją modulu).
- ✅ **Code review Fazy 1** (2026-05-28): severity gate **CZYSTE** — 0 P1, 0 P2, 4 P3 (opcjonalne nity + heads-up). Raport: `review-faza-1.md`. Manual test checklist: `manual-test-faza-1.md` (DEFERRED do Fazy 5 — 5 scenariuszy: toggle 3 trybów, persist po restarcie, dark mode parity ekranów, edge cases, two-device sync).
  - **Kluczowe wnioski**: (1) **Heads-up dla Fazy 2**: `(app)/_layout.tsx:11` nadal używa raw `useColorScheme()` zamiast `useEffectiveTheme()` — tab bar IGNORUJE manual override. `feature-builder-ui` Fazy 2 MUSI podmienić import w trakcie tab bar redesign. (2) Race hydratacji AsyncStorage: przed hydratacją Zustand persist `mode='system'`, więc po restarcie z zapisanym `'dark'` może wystąpić milisekundowy flash systemowego trybu → settle. Acceptable dla MVP, walidacja wizualna w Scenariuszu 2 manual checklist. (3) `useEffectiveTheme()` to pure derived hook — idealny kandydat na pierwszy unit test gdy projekt dostanie Jest + RNTL (5 cases: system×3 + override×2).
- ✅ **Faza 2 — Tab bar redesign** (2026-05-28): `(app)/_layout.tsx` zaktualizowany — `tabBarIcon` z lucide (`Home`/`Calendar`/`BarChart3`/`User`) dla 4 zakładek, `tabBarShowLabel: false` (ikony mówią same za siebie zgodnie ze screenem #1). **Wariant A wystarczył** — outlined chip dla active state uzyskany przez wrapper `<TabIcon>` w `tabBarIcon` (border 2px + rounded-pill + paddingHorizontal 14 / paddingVertical 6). **Heads-up review Fazy 1 zaadresowany**: `useColorScheme()` podmienione na `useEffectiveTheme()` z `ThemeProvider` — tab bar respektuje manual override. Kolory z palety: active `#1E1B4B` (navy) / `#F5F0E8` (cream) dark, inactive `#6B6580` (text-muted) / `#B8A8D9` (purple-light) dark. Walidacja: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe decyzje**: (1) `tabBarShowLabel: false` — screen #1 pokazuje TYLKO ikony w pill (chip otacza samą ikonę, label byłby redundantny); (2) `TabIcon` jako lokalny komponent (nie wynosimy do `src/components/ui/` — 1 użycie, YAGNI); (3) `LucideIcon` typ jako `ComponentType<{color,size,strokeWidth?}>` — match lucide-react-native API, zero `any`; (4) `strokeWidth` lekko bolder (2.25) dla focused — subtelny visual hint extra do borderu.
- ✅ **Code review Fazy 2** (2026-05-28): severity gate **CZYSTE** — 0 P1, 0 P2, 2 P3 (opcjonalne nity: dead-ternary `backgroundColor` na linii 49, brak explicit return type na `TabIcon`/`AppTabsLayout`). Raport: `review-faza-2.md`. Manual test checklist: `manual-test-faza-2.md` (4 scenariusze on-device — S1 ikony light/dark, S2 focus chip, S3 tap area ≥44pt, S4 iOS+Android parity; non-blocking, do wykonania równolegle / po Fazach 3-5). Walidacja re-runu: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe wnioski**: (1) **Pattern lucide named imports + `ComponentType<{color,size,strokeWidth?}>` typ** sprawdził się — stosować w `HomeHeader` (Faza 3, Bell), `QuickActions` (Sun/Moon/Plus), `SessionListItem` (Sun/Moon/ChevronRight), `Profil` (Bell/Moon/Gear). (2) `useEffectiveTheme()` używać wszędzie gdzie potrzeba `'light'|'dark'` (nie raw `useColorScheme`) — Faza 5 bottom sheet to konsumuje. (3) Lokalny `TabIcon` (1 użycie) — YAGNI sprawdziło się; wynosić do `src/components/ui/` tylko jeśli Faza 3/4/5 zażąda tego samego outlined-chip wzorca. (4) Dead-ternary nit do batch-fix w Fazie 6 polish.
- ✅ **Code review Fazy 3** (2026-05-28): severity gate **CZYSTE** — 0 P1, 0 P2, 5 P3 (wszystkie kosmetyczne / batch-fix Faza 6). Raport: `review-faza-3.md`. Manual test checklist: `manual-test-faza-3.md` (6 scenariuszy on-device — S1 HomeHeader, S2 ActiveWindowCard, S3 TodayStatsCard, S4 BigActionButton+QuickActions, S5 "Pokaż wszystkie" router.push, S6 dark mode parity iOS/Android; non-blocking). Walidacja re-runu: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe wnioski**: (1) **HEX literals przekroczyły próg "3+ duplikacji"** z Fazy 0 (9+ wystąpień `#1E1B4B`/`#F5F0E8`/`#E08B6F`/`#7C6BAD`/`#6B6580` w fazach 0+2+3) — czas wyciągnąć do `src/lib/colors.ts` w Fazie 6 polish. (2) **`ActiveWindowCard` bez `dark:bg-*` variantu** — świadoma decyzja designerska (tło `bg-orange-soft` zostaje w dark mode), ale do walidacji manual w S6 czy nie wygląda obco obok ciemnego tła. Jeśli FAIL — dodać `dark:bg-orange/20`. (3) `greetingForHour` używa `getHours()` w device tz — udokumentowany trade-off dla MVP single-user PL, do wyciągnięcia do `lib/time.ts` przy ekspansji geograficznej (zgodne z `learned-patterns.md` TZ-safe time). (4) Brak Jest setupu w projekcie — pure functions `greetingForHour` (8 boundary cases) + `computeAggregates` to idealne kandydaty na pierwsze testy gdy Jest dojdzie. (5) `useNotificationDot` ma explicit `: boolean` — wzorzec do naśladowania dla pure helperów (vs inferred return type komponentów React).
- ✅ **Faza 4 — Historia redesign** (2026-05-28): rewrite `history.tsx` + nowy helper `session-gaps.ts` + pelny rewrite `SessionListItem.tsx`. Header "Historia" + subtitle "Wszystkie sesje snu" (text-muted), `SegmentedControl` Lista (`List` icon) / Kalendarz (`Calendar` icon), Kalendarz to placeholder Card ("Widok kalendarza wkrótce" + Calendar 36px ikona). Widok Lista: pobiera ostatnie 14 dni, grupuje per `dayKeyInAppTz`, dni posortowane desc (najnowszy najpierw), wewnatrz dnia sesje posortowane asc (najwczesniejsza u gory — aby gapy "aktywnosci" mialy sens chronologiczny). Header dnia: tytul "Dzisiaj" / "Wczoraj" / "DD.MM" + agregat `Xg Ym · N sesji` (poprawna polska deklinacja `sesja`/`sesji`). Sesje wewnatrz jednej Card (rounded-card) z separatorem `border-t border-cream dark:border-dark-surface` miedzy itemami. `SessionListItem` rewrite: chip Sun/Moon (orange-soft / purple-soft bg, dark variants `bg-orange/30` / `bg-purple/30`), srodek zakres `HH:MM — HH:MM` (font-display lg bold) + kropka statusu + "Drzemka · 1g 11m", po prawej `ChevronRight`. Gap "aktywność Xg Ym" renderowany NAD itemem (gdy `gapBeforeMs > 0`) z lewa krawedzia `bg-orange/40` (px line). Helper `computeGapsBetweenSessions`: TZ-safe (input/output UTC ms, grupowanie po `dayKeyInAppTz`), zwraca `Map<sessionId, gapMs>`, pomija cross-day pary i sesje z `prev.end_at === null`. Walidacja: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Odchylenia od planu**: (1) **`MoreVertical` placeholder POMINIETY** — design.md/zadania.md wymienia go jako "visual only na lewej krawedzi karty", ale w praktyce design screen #2 pokazuje 3-kropki na **karcie**, nie na pojedynczym wierszu. Pominiety w Fazie 4 — moze byc dodany w Fazie 6 polish jesli wizualne porownanie wymagac bedzie. (2) **DatePickerField + tryb `day` USUNIETY** — design.md Faza 4 wprost zastepuje stary "wybierz dzien / ostatnie 14" przez `SegmentedControl` Lista/Kalendarz, gdzie Lista zawsze pokazuje ostatnie 14 dni. `DatePickerField` zostaje w projekcie (uzywany przez `BackdatedSessionModal`). (3) **`SectionList` → `ScrollView` + map** — przy malej liczbie sesji (14 dni × ~5/dzien = ~70) prostszy markup nizej kosztu jest OK, plus pozwala renderowac Card per dzien z separatorami wewnatrz, czego SectionList nie wspiera natywnie.
  - **Kluczowe decyzje**: (1) `SessionListItem.onPress` jako optional override — Historia przekazuje `router.push('/session/[id]')` jawnie, Dzisiaj korzysta z default `router.push` w komponencie. Backward-compat z `<SessionListItem session={s} />` w `index.tsx`. (2) `disableNavigation` zachowany dla potencjalnych przyszlych read-only kontekstow (statystyki). (3) Polski format daty `Dzisiaj/Wczoraj/DD.MM` — bez date-fns/locale (unika nowej zaleznosci tranzytywnej, locale `pl` JEST w `node_modules/date-fns/locale/pl` jako tranzytywna, ale uzycie wymagaloby dodatkowego importu — MVP simple). (4) `gapBeforeMs` w `SessionListItem` jako number (UTC ms) zamiast pre-formatted string — separation of concerns: helper computeGapsBetweenSessions zwraca ms, komponent formatuje przez `formatDuration` z `time.ts`. (5) Kierunkowosc gapow: sesje posortowane **rosnaco** wewnatrz dnia (najwczesniejsza na gorze) — gap = `prev.end_at` -> `next.start_at`, czyli "aktywnosc PRZED kolejna sesja" renderowany NAD nia.
- ✅ **Code review Fazy 6** (2026-05-28): severity gate **✅ CZYSTE** — 0 P1, 0 P2, 3 P3 (opcjonalne nity kosmetyczne). Raport: `review-faza-6.md`. Manual test checklist: `manual-test-faza-6.md` (8 scenariuszy on-device: S1 VoiceOver/TalkBack labels, S2 touch targets ≥44pt + hitSlop, S3 tabular nums, S4 pressable feedback consistency, S5 ProgressRing fade-in mount-only, S6 dark mode WCAG AA wszystkie ekrany, S7 SegmentedControl animation regression check, S8 Realtime sync regression; non-blocking). Walidacja CLI: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe wnioski**: (1) **Brak regresji SegmentedControl** — komponent NIE zmodyfikowany w Fazie 6, oryginalna `withTiming` animacja indicatora zachowana, Pressable feedback dodany tylko na 7 callsiteach NIE pokrywajacych SegmentedControl. (2) **HEX 1:1 zachowane**: wszystkie 6 wartosci w `COLORS` zgodne z `tailwind.config.js`. Dark mode tokens `#0F0D26`/`#2A2660` swiadomie pozostaja inline w `_layout.tsx` (jeszcze nie w COLORS). (3) **ProgressRing fade-in mount-only**: `useEffect([opacity])` — stabilny ref, effect nie re-fire przy value updates. dashOffset to declarative SVG, instant render. **Brak konfliktu z rerenderami.** (4) **`accessible={false}` nie nadmiernie applied** — tylko raz, na stop-propagation Pressable z no-op `onPress` (`ThemeModeBottomSheet.tsx:64`). 3 wewnetrzne Pressable opcji motywu nadal accessible. (5) **hitSlop bez overlapping** — back button (8pt) i "Pokaz wszystkie" (8pt) sasiaduja tylko z non-interactive text. IconButton `size=sm` z hitSlop 6 — defensywne, brak callsiteów aktualnie. (6) **Odchylenia od planu zaakceptowane**: (a) FlatList refaktor odlozony z TODO inline (`RANGE_DAYS=14` × ~5 sesji = ~70 itemów, ScrollView OK, refaktor wymaga restrukturyzacji GroupedHistoryList — out of MVP scope); (b) "return types skip" — faktycznie nic do dodania, wszystkie publiczne funkcje mają explicit types, komponenty React inferują JSX.Element jak reszta bazy. (7) **3 P3 sugestie**: `purple-soft` (#E8DEF7) inline w Switch/ProgressRing (dodać do COLORS przy nast. modyfikacji palety, redukcja 22→20), stop-propagation Pressable → View+responder (kosmetyka), opacity-vs-scale feedback rule do udokumentowania w kontekście ("scale dla CTA/cards, opacity dla list rows/secondary").
- ✅ **Faza 6 — Polish + a11y** (2026-05-28): batch fix akumulowanych P3 z Faz 0-5 + checkboxy "Implementacja" Fazy 6. **Utworzony `src/lib/colors.ts`** (`COLORS = { navy, cream, orange, purple, purpleLight, textMuted } as const`) — refaktor 14 plikow z 50 do 22 wystapien HEX literals (`_layout`, `history`, `profile`, `HomeHeader`, `SessionListItem`, `QuickActions`, `BigActionButton`, `IconButton`, `Switch`, `settings`, `ThemeModeBottomSheet`, `index`, `ProgressRing`, `ActiveWindowCard`). Pozostale 22 wystapienia w auth/family/sessions formach (poza scope'em redesignu — `placeholderTextColor`, `ActivityIndicator` w `AddChildForm`/`BackdatedSessionModal`/`SessionEditForm`/auth screens). **P3 batch-fix**: (a) `_layout.tsx` dead ternary `isDark ? 'transparent' : 'transparent'` usuniety + `isDark` prop wyciety z `TabIcon`; (b) `ActiveWindowCard` redundancja `dark:text-navy` usunieta; (c) `TodayStatsCard` local re-alias `dayMs = MS_PER_DAY` usuniety; (d) `history.tsx` `setDate(getDate() - N)` → `addDays(createdAt, -(RANGE_DAYS - 1))` z `date-fns` (TZ-safe pattern z `learned-patterns.md`); (e) `dayTitleFor` `yesterday.setDate(getDate()-1)` → `addDays(now, -1)`; (f) `SessionListItem.accessibilityLabel` rozszerzony o ", po aktywnosci Xg Ym" gdy `showGap`; (g) `ThemeModeBottomSheet` inner Pressable stop-propagation z `accessible={false}` (a11y); (h) `profile.ShortcutRow` `useEffectiveTheme()` per-row → `chevronColor` propsem od rodzica. **Faza 6 implementacja**: (1) `IconButton` defaults `iconColor = COLORS.navy`, dodany `hitSlop` dla `size='sm'` (36pt → 44pt+ tap area), Pressable feedback (`scale 0.97 + opacity 0.85` na press); (2) `Avatar` juz mial `accessible accessibilityRole="image" accessibilityLabel={name}` z Fazy 0 — verified; (3) `SleepInProgressCard` timer + `short` z `fontVariant: ['tabular-nums']`; (4) `ProgressRing` fade-in opacity 0→1 nad 300ms (Reanimated `useSharedValue` + `useAnimatedStyle`), label tez z `tabular-nums`; (5) Pressable feedback dodany w: `BigActionButton`, `QuickActions.ActionCard`, `SessionListItem`, `profile.ShortcutRow`, `settings` back+Wyloguj, `index` Pokaz-wszystkie+InvitationRow; (6) `BigActionButton` dodany `accessibilityLabel`; (7) `index.InvitationRow` Pressable dodany `accessibilityLabel` z `family_name`; (8) `settings` back-button `w-10 h-10` → `w-11 h-11` + `hitSlop` (≥44pt tap area). **ScrollView w `history.tsx`** zostal — dodany inline TODO comment dla future FlatList refaktoru przy >100 sesji (>30 LOC scope, odlozone). Walidacja: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe decyzje**: (1) **`colors.ts` w `src/lib/`** — alignuje z `time.ts` (utility module), zero deps. Komentarz w pliku zaznacza ze `tailwind.config.js` jest collegial source-of-truth (zmiana w jednym miejscu wymaga sync w drugim — niestety RN nie pozwala na deep import tokenow tailwinda bez build stepu). (2) **Pressable feedback przez inline `style={({pressed}) => ...}` (nie Reanimated)** — KISS dla pojedynczych komponentow, jeden pattern dla ~7 callsiteow. Reanimated wniosloby per-component shared values bez benefit (animacja transformu wystarczajaco gladka bez worklet). (3) **ProgressRing fade-in PRZEZ Reanimated** (nie inline `Animated`) — `useSharedValue` + `withTiming` na opacity ma 0 cost przy re-renderach, ekran Dzisiaj odswieza sie co 30s. (4) **HEX migration scope tradeoff** — w pelni refaktorowac wymagaloby 21 plikow (>15 threshold z planu), wiec przykorzystano do 14 plikow z core UI redesignu (Faza 0-5 callouts) i odlozono auth/family forms (placeholderTextColor + ActivityIndicator color, te same HEXy ale w out-of-scope plikach). (5) **`hitSlop` zamiast zwiekszenia padding** — zachowuje visual layout, rozszerza tap area niewidocznie (iOS pattern). (6) **Pressable feedback NIE dotyka istniejacych `onPress`/`disabled`** — tylko dodaje `style` callback, brak ryzyka regresji logiki.
- ✅ **Faza 5 — Profil redesign** (2026-05-28): rewrite `profile.tsx` + 2 nowe helpery (`child-age.ts`, `sleep-stats.ts`) + `settings.tsx` placeholder route + `ThemeModeBottomSheet.tsx`. Header "Profil" + subtitle "Dzieci i ustawienia" + gear `IconButton` → `router.push('/settings')`. Karta aktywnego dziecka: solid `bg-purple-light` (decyzja Fazy 0, NIE gradient), Avatar lg z border-2 border-white, imie text-2xl bold, `formatChildAge` (np. "21 miesiecy · ur. 12 sie. 2024"). Zagniezdzona biala karta: label "NORMA SNU DLA WIEKU" uppercase + value "X-Yg/dobe" z `getNormForChild`, `ProgressBar` (avgSleep7d / norm.maxHours, tint success jesli >=85% normy, orange ponizej), tekst "Srednio Xg Ym ostatnie N dni · Y% normy". Sekcja SKROTY: 2 wiersze (Bell "Przypomnienia"/"Wlaczone" placeholder, Moon "Tryb ciemny"/label trybu) w jednej Card z separatorem. Tap row 2 otwiera `ThemeModeBottomSheet` (RN `Modal` transparent+slide, 3 opcje Smartphone/Sun/Moon + `Check` dla aktywnej, backdrop tap zamyka). Sekcja "Rodzina" + Wyloguj **przeniesiona do `(app)/settings.tsx`** (full feature parity — `FamilyMembersList`, `InviteMemberForm`, `PendingInvitationsList`, `NoFamilyFallback`, sign out). Settings route ukryta w tab barze przez `<Tabs.Screen name="settings" options={{ href: null }} />` (pattern z `sleep-fullscreen`/`session/[id]`). Walidacja: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe decyzje**: (1) **Tri-state bottom sheet via RN `Modal`** — KISS, brak nowych zaleznosci (`@gorhom/bottom-sheet` nie jest w projekcie, instalacja wymagalaby zatwierdzenia §8 a YAGNI dla pojedynczego sheeta). Pattern stop-propagation: zewnetrzny Pressable (backdrop) + wewnetrzny Pressable (sheet, no-op onPress) — tap-outside zamyka, tap na sheet nie. (2) **Routing `/settings` via `<Tabs.Screen href: null>`** zamiast wynoszenia poza grupe `(app)` — wymaga 1 linii zmiany w `_layout.tsx`, route push-able przez `router.push('/settings')`, ukryty w tab barze, taki sam pattern jak `sleep-fullscreen`/`session/[id]`. Stack.Screen w root layout byloby narzutem (zmienialibysmy auth-aware layouting). (3) **`useAvgSleep7d` range = 7 pelnych dni WSTECZ (bez dzisiaj)** — dzisiaj to niepelny dzien (sesja w toku albo brak nocy), wlaczenie zanizyloby srednia. `dayKeyInAppTz` per-day aggregation z splitem sesji cross-midnight (`durationWithinWindow`). `useMemo([todayKey])` stabilizuje rangeStart/rangeEnd referencje — bez tego `new Date()` per render generowalby nowe ISO i nieskonczony refetch w `useSessions` (queryKey zawiera `toISOString()`). (4) **`formatChildAge` TZ-safe** — wiek liczony jako UTC ms diff (kazda doba = 24h, wiek to "ile minelo"), ale data urodzenia formatowana przez `toZonedTime(APP_TIMEZONE)` — uzytkownik widzi date w Warsaw niezaleznie od device tz (zgodne z `learned-patterns.md` TZ-safe time). Tabela polskich miesiecy short (`sty/lut/.../gru`) bo `date-fns` locale `pl` wymagaloby ekstra importu (`date-fns/locale/pl`) — YAGNI dla 12 stringow. (5) **Threshold "w normie" = 85% maxHours** — design.md pisze "X% w kolorze success jesli >=85%, orange jesli <85%". Tint ProgressBar i text procentu sync. (6) **Sign out PRZENIESIONY**, nie zduplikowany — pojedynczy entry point z settings.tsx, profile.tsx nie ma sign out. Funkcjonalnosc zachowana, user dociera przez gear icon. (7) **NoActiveChildCard fallback** — zachowanie z poprzedniej wersji (brak rodziny/dziecka), zamiast crashy: pokazuje karta "Brak aktywnego dziecka" z emailem zalogowanego. (8) **`ShortcutRow` jako lokalny komponent** — 2 uzycia w pliku, YAGNI dla wynoszenia do `src/components/ui/` (analogia do `TabIcon` z Fazy 2).
- ✅ **Code review Fazy 5** (2026-05-28): severity gate **CZYSTE** — 0 P1, 0 P2, 5 P3 (design-question + a11y-nit + 3 batch-fix kandydaci Faza 6). Raport: `review-faza-5.md`. Manual test checklist: `manual-test-faza-5.md` (5 scenariuszy on-device — S1 norma snu różny wiek, S2 średnia 7d, S3 toggle dark mode tri-state + persist + backdrop, S4 dark mode parity, S5 back navigation /settings + sign out flow; non-blocking). Walidacja CLI: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe wnioski**: (1) **Sign out flow zachowany** — `supabase.auth.signOut()` w `settings.tsx:30`, `AuthProvider` subskrybuje `onAuthStateChange` → `Redirect` w `(app)/_layout.tsx`. Pojedynczy entry point (KISS), brak duplikacji. (2) **`useAvgSleep7d` semantic decision** — `daysCovered = liczba dni aktywnych` (nie 7), label "ostatnie N dni" honestyfikuje, ale design.md mówi "ostatnie 7 dni · Y% normy". Decyzja produktowa po manual S2: zostawić (fair dla sparse trackera) vs `sumMs/7` (motywuje codzienny tracking). Klasyfikacja P3 [design-question]. (3) **TZ-safety potwierdzona**: `useAvgSleep7d` używa `dayKeyInAppTz`/`startOfDayInAppTz`/`endOfDayInAppTz`, `formatChildAge` używa `toZonedTime` na renderingu daty urodzenia. Zgodne z `learned-patterns.md`. (4) **Brak N+1**: `useSessions(childId, rangeStart, rangeEnd)` to **jeden Supabase SELECT** dla całego okna, agregacja per-day client-side. `useMemo([todayKey])` zapobiega refetch petli. (5) **WCAG AA potwierdzony**: navy (#1E1B4B) na purple-light (#B8A8D9) ≈ 7.0:1 (PASS dla normal text). Dark mode S4 manual verify. (6) **Polskie liczebniki w `formatChildAge`**: pełna obsługa edge cases (1=miesiac, 2-4=miesiace, 5+=miesiecy, 12-14 lastTwo branch, podobnie weeks/years). (7) **Tri-state bottom sheet via RN Modal**: KISS, bez nowych deps. P3 a11y nit: inner Pressable stop-propagation może być widoczny dla VoiceOver — Faza 6 a11y batch.
- ✅ **Code review Fazy 4** (2026-05-28): severity gate **CZYSTE** — 0 P1, 0 P2, 5 P3 (wszystkie nity / batch-fix kandydaci do Fazy 6 polish). Raport: `review-faza-4.md`. Manual test checklist: `manual-test-faza-4.md` (4 scenariusze on-device — S1 gap calc same-day, S2 tap session detail, S3 calendar placeholder, S4 dark mode parity; non-blocking, plus opcjonalne S5-S7 edge cases). Walidacja re-runu: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe wnioski**: (1) **TZ-safety potwierdzona**: `computeGapsBetweenSessions` uzywa `dayKeyInAppTz` do grupowania per dzien, gap liczony jako UTC ms diff — zgodne z `learned-patterns.md` TZ-safe time pattern. Cross-day pary pomijane intencjonalnie (= nowa sekcja w UI). (2) **Wszystkie 4 odchylenia z kontekstu zaakceptowane bez korekty** — `MoreVertical` POMINIETO (visual decision; rozwazyc Faza 6 jesli manual test wykaze brak parity ze screenem), `DatePickerField` usuniety z `history.tsx` (zgodne z planem, wciaz uzywany w `SessionEditForm.tsx`), `SectionList → ScrollView` (uzasadniona pragmatyka, separator wewnatrz Card per dzien), `DD.MM` format (pragmatyczne MVP, brak importu `pl` locale). (3) **5 P3 do batch-fixa w Fazie 6**: `setDate` → `addDays` w `history.tsx:57-58` (alignment z TZ-safe pattern), `accessibilityLabel` bez gap context w `SessionListItem.tsx:120`, ScrollView vs FlatList przy skalowaniu, MoreVertical placeholder, cross-midnight UI clarity. (4) **Pure function `computeGapsBetweenSessions`** to kolejny kandydat (po `greetingForHour`, `computeAggregates`, `getNormForChild`) na pierwszy unit test gdy projekt dostanie Jest — 7 boundary cases (empty, single, 2 same-day, 2 cross-day, prev.end_at=null, gap<=0, unsorted input).
- ✅ **Faza 3 — Dzisiaj redesign** (2026-05-28): Restyle ekranu Dzisiaj (`(app)/index.tsx`) zgodnie ze screenem #1 — 5 istniejących komponentów + 1 nowy `HomeHeader` + 1 helper `useNotificationDot.ts`. `HomeHeader` (Avatar + greeting per godzina z 4-stanowym mapem 5-12/12-18/18-23/23-5, ChevronDown visual, Bell `IconButton` z dot `mock=true`). `ActiveWindowCard` — solid `bg-orange-soft` (gradient SKIP wg decyzji Fazy 0), kropka+label "OKNO AKTYWNOŚCI", timer `text-6xl tabular-nums`, ProgressBar tinted orange, footer "Pobudka o HH:MM" + Badge "Drzemka za ~Xg Ym". `TodayStatsCard` — `ProgressRing` z procentem (total/recommendedHours), `ProgressBarStacked` (purple/orange/success segmenty doby), 3 mini-statystyki z kropkami. `BigActionButton` — opcjonalny `sessionType` prop (default `'nap'`), Moon ikona dla `start+night_sleep`. `QuickActions` — 3 białe karty z okrągłymi ikonowymi chipami (Sun orange-soft, Moon purple-soft, Plus cream/dark-surface). `index.tsx` — `<HomeHeader child={activeChild} />` (fallback do "Dzisiaj" gdy `activeChild` null, np. brak rodziny lub przed dodaniem dziecka), "Pokaż wszystkie" jako Pressable z `router.push('/history')`. Walidacja: `tsc --noEmit` PASS, `expo lint` PASS.
  - **Kluczowe decyzje**: (1) `SessionListItem` NIE modyfikowany w Fazie 3 — design.md i task.md eksplicytnie pozwalają zostawić bez gapów (komponent juz ich nie ma); prop `gapBeforeMs?` dojdzie w Fazie 4 razem z rewrite (sun/moon ikona + zakres dużą czcionką + ChevronRight). YAGNI sprawdziło się. (2) `TodayStatsCard.recommendedHours` jako opcjonalny prop z default `13` — design.md zaznacza "z 13g zalecanych", przyszle podłączenie pod `getNormForChild(birthDate).maxHours` to scope Fazy 5 (data flow w Fazie 3 niezmieniony). (3) `BigActionButton.sessionType` jako opcjonalny prop default `'nap'` — call site przekazuje `activeSession?.type ?? 'nap'`, Moon nie pokazuje się dla domyślnego nap-startu (call site nadal woła `handleStart('nap')`); Moon zaświeci się gdy user kiedyś przepina default na night. (4) `useNotificationDot` jako prosty `() => true` w `src/lib/` (nie hook z `useState`) — mock z decyzji Fazy 0, podłączenie pod `expo-notifications` poza scope'em redesignu. (5) `HomeHeader` używa `useEffectiveTheme()` do iconColor (Bell + ChevronDown) — spójne z patternem Fazy 2 (lucide nie czyta className cross-platform). (6) "Pokaż wszystkie" przeszło z `<Link>` na `<Pressable onPress={router.push}>` — task.md eksplicytnie tego wymaga (lepsze a11y: accessibilityRole/Label, możliwość przyszłego haptic feedback).

## Powiązane pliki

### Modyfikowane (11)

| Plik | Cel zmiany | Faza |
|---|---|---|
| `sleeper-app/tailwind.config.js` | Kolory, radii, shadows, `darkMode: 'class'`, fontFamily | 0, 1 |
| `sleeper-app/src/app/_layout.tsx` | Mount `ThemeProvider` powyżej `Stack` | 1 |
| `sleeper-app/src/app/(app)/_layout.tsx` | `tabBarIcon` z lucide (Home/Calendar/BarChart3/User) | 2 |
| `sleeper-app/src/app/(app)/index.tsx` | Nowy `HomeHeader`, restyle wewnętrznych komponentów | 3 |
| `sleeper-app/src/app/(app)/history.tsx` | Header + subtitle, SegmentedControl, grupowane sekcje | 4 |
| `sleeper-app/src/app/(app)/profile.tsx` | Rewrite — karta dziecka + SKRÓTY (sekcja Rodzina → /settings) | 5 |
| `sleeper-app/src/components/ActiveWindowCard.tsx` | Soft bg, ProgressBar, Badge "Drzemka za" | 3 |
| `sleeper-app/src/components/TodayStatsCard.tsx` | ProgressRing 98%, stacked bar, mini-statystyki z kropkami | 3 |
| `sleeper-app/src/components/BigActionButton.tsx` | Moon ikona przed labelem dla night start | 3 |
| `sleeper-app/src/components/QuickActions.tsx` | 3 białe karty z okrągłym ikonowym chipem (Sun/Moon/Plus) | 3 |
| `sleeper-app/src/components/SessionListItem.tsx` | Ikona sun/moon, zakres godzin large, ChevronRight, prop `gapBeforeMs?` | 3, 4 |

### Nowe (15+)

**Primitives UI** (`sleeper-app/src/components/ui/`, Faza 0):
- `Avatar.tsx` — kółko z color bg + inicjał + opcjonalny image, sizes sm/md/lg
- `Card.tsx` — wrapper `bg-white dark:bg-dark-card rounded-card p-5 shadow-card`, wariant `gradient?: 'purple'|'orange'`
- `Badge.tsx` — pill kształt, warianty success/neutral/orange
- `IconButton.tsx` — round button z lucide ikoną (dla bell, gear)
- `ProgressBar.tsx` — płaski rounded, prop `value: 0..1`, `tintClassName?`
- `ProgressBarStacked.tsx` — segments `{ value, className }[]`
- `ProgressRing.tsx` — SVG ring (react-native-svg), prop `value, size, strokeWidth, label?`
- `SegmentedControl.tsx` — iOS-style pill z animacją Reanimated `useSharedValue`
- `Switch.tsx` — wrapper na RN `<Switch>` z naszymi kolorami (lub custom Reanimated)

**Komponenty wyższego rzędu:**
- `sleeper-app/src/components/HomeHeader.tsx` — Avatar + greeting (time-of-day) + bell IconButton (Faza 3)
- `sleeper-app/src/components/CustomTabBar.tsx` — *opcjonalny* (wariant B Fazy 2, jeśli `tabBarIcon` nie wystarczy)

**Theme system** (`sleeper-app/src/features/settings/`, Faza 1):
- `useThemeStore.ts` — Zustand + AsyncStorage persist, state: `mode: 'system'|'light'|'dark'`
- `ThemeProvider.tsx` — czyta mode + `useColorScheme()`, liczy `effectiveTheme`, opakowuje children w `className="dark"` na root View

**Helpers** (`sleeper-app/src/lib/`, Faza 0/4/5):
- `sleep-norms.ts` — `getNormForChild(birthDate, now)` → `{ minHours, maxHours, label }`. Tabela wiekowa (decyzja Fazy 0)
- `sleep-stats.ts` — `useAvgSleep7d(childId)` hook, TanStack Query, reuse `useSessions`
- `child-age.ts` — `formatChildAge(birthDate)` → "21 miesięcy" lub "2 lata · ur. DD MMM RRRR"
- `session-gaps.ts` — `computeGapsBetweenSessions(sessions)` → "aktywność Xg Ym" między `prev.end_at` i `next.start_at`

## Decyzje techniczne

1. **`darkMode: 'media' → 'class'`** — manual override z 3 trybami. Wszystkie istniejące `dark:` classes działają jak były, ale teraz driven by `className="dark"` na root View.
2. **Single-child first** — header bez dropdown switchera (avatar+imię jako label), Profil bez sekcji "DZIECI".
3. **Bell + Przypomnienia visual only** — placeholdery, brak `expo-notifications` flow w tej rundzie.
4. **Statystyki out of scope** — ekran `(app)/stats.tsx` pozostaje bez zmian.
5. **expo-router `Tabs`** (nie `NativeTabs`) — utrzymujemy konwencję MVP, dodajemy tylko `tabBarIcon` z lucide.
6. **`react-native-svg` dla ProgressRing** — solidne, cross-platform. Alternative (2 nakładające View z `transform: rotate`) odrzucone jako fragile.
7. **Reanimated `withTiming` 200ms** dla SegmentedControl — bez `withSpring` (zacinanie na Android).

## Dependencies (3 nowe, regula §8 coding-rules)

| Dep | Cel | Status | Rekomendacja |
|---|---|---|---|
| `lucide-react-native` | Ikony cross-platform | DO ZATWIERDZENIA przez usera | Install — ~10KB tree-shaken |
| `react-native-svg` | ProgressRing | Sprawdzić `npm ls react-native-svg` (może już być tranzytywnie via expo-router) | Install tylko jeśli brak |
| `expo-linear-gradient` | Gradient karty dziecka | DO ZATWIERDZENIA | **SKIP** — użyć solid `bg-purple-light` (alternatywa zaakceptowalna wizualnie) |

User MUSI zatwierdzić instalację przed `npm i` (regula §8).

## Designerski kontekst

- **DESIGN.md (projekt-wide):** `design.md` w root projektu (NIE klasyczna struktura `docs/design/` — ten plik pełni rolę DESIGN.md dla tego zadania)
- **SPEC.md (per-feature, pomiary z Figmy):** `null` — brak Figmy w tym zadaniu (design opisowy + referenced screeny #1/#2/#3 w tekście `design.md`)
- **Screeny referencyjne:** lista pusta (user wskazał screeny ustnie w sesji `/design` poprzedzającej; brak persisted PNG w repo)

> **Subagenci buildujący UI w Fazach 3/4/5 dostają `design.md` jako MANDATORY context.** `dev-docs-execute` wstrzykuje ten plik do promptu Agent tool.

## Zależności faz

```
Faza 0 (foundation)
   ↓
Faza 1 (dark mode) — wymaga `darkMode: 'class'` z Fazy 0
   ↓
Faza 2 (tab bar) — wymaga `lucide-react-native` z Fazy 0
   ↓
Faza 3 (Dzisiaj) ─┐
Faza 4 (Historia) ─┼── równolegle dopuszczalne (różne pliki, wspólne primitives już gotowe)
Faza 5 (Profil) ──┘
   ↓
Faza 6 (polish + a11y)
   ↓
Faza 7 (manual test)
```

## Rekomendowane skille / agenci

**Przed implementacją:**
- `ux-ui-guidelines` — przy projektowaniu primitives Fazy 0 (a11y, concentric radius, touch targets)
- `tailwind-react-guidelines` — każda faza UI (NativeWind v4 patterns)

**Implementacja per faza:**
- `feature-builder-ui` (agent) — fazy 0/2/3/4/5/6 (UI only)
- `feature-builder-fullstack` (agent) — Faza 1 graniczna; prościej trzymać w `feature-builder-ui` z drobnym Zustand store

**Po każdej fazie:**
- `code-review` — review przed merge fazy
- `code-quality` — po Fazie 6, audyt całości

**Manual testing:**
- `expo-rn-testing` — Faza 7 checklist
- `mobile-feature-tester` (agent) — generowanie listy testów per faza

**Po ukończeniu:**
- `dev-docs-complete` — archiwizacja
- `dev-compound` — nauki do `docs/solutions/` (dark mode 'class' migration, ProgressRing w RN)

## Otwarte pytania (do uzgodnienia w Fazie 0)

Wszystkie 5 są w `ui-redesign-zadania.md` jako blokery Fazy 0 z prefixem `Decyzja:`. Rekomendacje z `design.md`:

1. Sleep norm source → **WHO+AAP hybrid** (do potwierdzenia)
2. Gradient karty dziecka → **solid `bg-purple-light`** (SKIP `expo-linear-gradient`)
3. Sekcja "Rodzina" w Profilu → **`/settings` placeholder route** za gear icon
4. Toggle dark mode → **tri-state bottom sheet** (System/Light/Dark, iOS-idiomatic)
5. Bell icon dot → **mock `true`** (matchuje screen #1)

## Źródła

- Requirements doc: `null` (brak `docs/brainstorms/`)
- Plan techniczny: `null` (brak `docs/plans/`)
- **Source-of-truth:** `design.md` w root projektu (commit w tym branchu)
