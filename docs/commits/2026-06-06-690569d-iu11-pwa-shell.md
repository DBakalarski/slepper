# 690569d: feat(sleeper-web-pwa): IU11 PWA shell + faza 1/2 P2 hardening

**Data:** 2026-06-06
**Branch:** feature/sleeper-web-pwa
**Faza zadania:** Faza 4 / IU11 PWA shell

## Co zostalo zrobione

### IU11 PWA shell
- `public/manifest.json` — PWA standard (name "Sleeper", display standalone, theme #208AEF, icons 192/512 maskable, orientation portrait, lang pl).
- `public/sw.js` (~75 LOC) — Service Worker: cache-first dla shell + static (`/_expo/`, `/icons/`, `/manifest.json`), skip dla Supabase API (`/rest/v1/`, `/auth/v1/`, `/realtime/v1/`, `/storage/v1/`, `*.supabase.co`), offline SPA fallback do `/`.
- `public/index.html` — custom template (Expo `getFileFromLocalPublicFolder` zaadaptuje go nad domyslny): viewport-fit=cover, manifest link, theme-color (+ dark variant), apple-mobile-web-app-* meta tagi, apple-touch-icon 180×180, FOWT prevention przez `body { background-color: #F5F0E8; }` + dark media query.
- `public/icons/{icon-192, icon-512, apple-touch-icon}.png` — generowane via `sips` z `packages/sleeper-app/assets/images/icon.png` (1024×1024). `public/favicon.png` — kopia 48×48.
- `public/icons/README.md` — instrukcja regeneracji (sips / ImageMagick).
- `src/features/pwa/registerSW.ts` — idempotentny rejestrator SW: `typeof window/navigator` guards, czeka na `window.load`, error przez `console.error`.
- `src/app/_layout.tsx` — `useEffect(() => registerSW(), [])` post-mount.
- `app.json` — dodane `web.lang: "pl"`, `web.themeColor: "#208AEF"`, `web.description`.

### P2 hardening (deferred z Faz 1+2 — fix przed deploy prod)
- **Faza 1 P2.1** — `lib/supabase.ts`: `flowType: 'pkce'` w auth config (eliminuje token leak w URL fragment).
- **Faza 1 P3** — `lib/supabase.ts`: throw w `NODE_ENV=production` gdy brak env (Vercel misconfig fail-loud).
- **Faza 1 P2.2** — `app/(auth)/sign-in.tsx`: walidacja parytet sign-up (`isValidEmail`, `MIN_PASSWORD`, FormErrors state, handleSubmit) + `maxLength={254}` (email), `maxLength={128}` (password). Sign-up dostal te same `maxLength` dla parytetu.
- **Faza 1 P2.4** — `features/settings/useThemeStore.ts`: `webLocalStorage` adapter z `Platform.OS === 'web'` guard. Synchroniczny localStorage eliminuje FOWT.
- **Faza 1 P3 (security fallback)** — `features/auth/translate-auth-error.ts`: fallback message zmieniona z raw Supabase error na generic "Nie udalo sie. Sprobuj ponownie." (nie wycieka PostgREST hint / infra info).
- **Faza 1 P2.7** — `src/features/auth/__tests__/translate-auth-error.test.ts` — 10 cases (6 branchy + fallback security + edge).
- **Faza 1 P3 invariant** — `scripts/check-no-native-imports.sh` (egzekwuje brak `expo-notifications` w `lib/notifications.ts` i `schedule-nap-side-effects.ts`).
- **Faza 2 P2.3** — `babel.config.js`: `babel-plugin-transform-remove-console` z `exclude: ['error']` pod `NODE_ENV=production`. Eliminuje wszystkie `console.warn/log` leaki naraz (Faza 1 supabase warn, Faza 2 hooks.ts:293, Faza 3 P3.2 wszystko pokryte).

### Testy
- **+ 10** cases dla `translate-auth-error.test.ts` (Faza 1 P2.7)
- **+ 29** cases dla `registerSW.test.ts` (registerSW guards/scope/load + sw.js cache strategy/Supabase skip + manifest.json contract + index.html template invariants)
- Razem: **119 → 158** tests, wszystkie PASS.

## Zmienione pliki
- `packages/sleeper-web/public/{manifest.json, sw.js, index.html, favicon.png}` — NEW PWA shell assets
- `packages/sleeper-web/public/icons/{icon-192, icon-512, apple-touch-icon}.png + README.md` — NEW
- `packages/sleeper-web/src/features/pwa/{registerSW.ts, __tests__/registerSW.test.ts}` — NEW
- `packages/sleeper-web/src/features/auth/__tests__/translate-auth-error.test.ts` — NEW
- `packages/sleeper-web/scripts/check-no-native-imports.sh` — NEW
- `packages/sleeper-web/app.json` — web.{lang, themeColor, description}
- `packages/sleeper-web/babel.config.js` — production-only transform-remove-console plugin
- `packages/sleeper-web/package.json` — devDeps: babel-plugin-transform-remove-console
- `packages/sleeper-web/src/app/_layout.tsx` — registerSW() w useEffect
- `packages/sleeper-web/src/app/(auth)/sign-in.tsx` — walidacja parytet + maxLength
- `packages/sleeper-web/src/app/(auth)/sign-up.tsx` — maxLength (parytet)
- `packages/sleeper-web/src/features/auth/translate-auth-error.ts` — generic fallback
- `packages/sleeper-web/src/features/settings/useThemeStore.ts` — webLocalStorage adapter
- `packages/sleeper-web/src/lib/supabase.ts` — flowType pkce + prod throw

## Powod / kontekst
Faza 4 IU11 dostarcza PWA shell (manifest + SW + iOS meta + ikony) wymagany do "Add to Home Screen" + standalone mode na iPhone Safari. Skoroszyt: addresuje wszystkie deferred P2 z Faz 1+2 (security + perf hardening) PRZED deploy prod, zeby pierwsza wersja na Vercel byla "deployable as-is".

### Odchylenia od planu
- **`+html.tsx` zastapione przez `public/index.html`** — plan zakladal `+html.tsx` (Expo Router static rendering), ale my mamy `web.output: "single"` (Faza 2 P1 fix dla SSR crash z `window` w lib/supabase.ts). W trybie `single` Expo NIE procesuje `+html.tsx`; zamiast tego `createTemplateHtmlAsync` czyta `public/index.html` z placeholderami `%LANG_ISO_CODE%` / `%WEB_TITLE%` (z `web.lang` + name z `app.json`). Funkcjonalnie rownowazne.

## Walidacja
- `pnpm --filter sleeper-web exec tsc --noEmit` — PASS (0 errors)
- `pnpm --filter sleeper-web lint` — PASS (0 errors, 0 warnings)
- `pnpm --filter sleeper-web test` — PASS (14 files, 158/158 tests)
- `pnpm --filter sleeper-web build` — PASS (`dist/manifest.json`, `dist/sw.js`, `dist/icons/`, `dist/index.html` z PWA meta tagami)
- `grep -c "import.meta" dist/_expo/static/js/web/entry-*.js` — 0 (zustand CJS fix zachowany)
- `bash scripts/check-no-native-imports.sh` — PASS (oba pliki bez expo-notifications)
- runtime: pending — manual mobile test (Lighthouse + Add to Home Screen) na iPhone w fazie deploy (IU12)
