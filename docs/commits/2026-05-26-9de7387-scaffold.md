# 9de7387: feat(setup): scaffold Expo app with TS strict, NativeWind, TanStack Query

**Data:** 2026-05-26
**Branch:** feature/mvp-sleep-tracker
**Faza zadania:** Faza 0 — Setup projektu

## Co zostalo zrobione
- Scaffold Expo SDK 56 przez `npx create-expo-app@latest sleeper-app -t default` (RN 0.85, React 19.2, TS 6 — pozniej downgrade w commicie 12bffeb)
- TypeScript strict ON (z template), path alias `@/*` -> `./src/*`
- NativeWind v4.2 + Tailwind v3.4 (peer dep nativewind wymaga v3.x; Tailwind v4 ma niekompatybilny CSS-first config)
- Konfiguracja:
  - `tailwind.config.js` z paleta MVP (cream `#F5F0E8`, navy `#1E1B4B`, orange `#E08B6F`, purple `#7C6BAD`)
  - `babel.config.js` z `babel-preset-expo` (jsxImportSource: nativewind) + `nativewind/babel`
  - `metro.config.js` z `withNativeWind(config, { input: './src/global.css' })`
  - `src/global.css` z `@tailwind base/components/utilities`
  - `nativewind-env.d.ts` z `/// <reference types="nativewind/types" />`
- Supabase: `src/lib/supabase.ts` (AsyncStorage persistence, `detectSessionInUrl: false`, polyfill URL)
- TanStack Query: `src/lib/query-client.ts` + provider w `src/app/_layout.tsx`
- Routing: grupa `(app)` z 4 tabami:
  - `index.tsx` (Dzisiaj), `history.tsx`, `stats.tsx`, `profile.tsx` — placeholdery
  - `(app)/_layout.tsx` uzywa `Tabs` z `expo-router` (nie `NativeTabs` — wymaga PNG icons per tab)
- `.env` (puste wartosci) + `.env.example` (placeholdery) + `.gitignore` (`.env` ignorowany, `!.env.example` wyjatek)

## Zmienione pliki
- 65 plikow utworzonych w `sleeper-app/` (template + nasze nadpisania)
- `docs/active/mvp-sleep-tracker/mvp-sleep-tracker-zadania.md` — odznaczono 10/13 checkboxow Fazy 0
- `docs/active/mvp-sleep-tracker/mvp-sleep-tracker-kontekst.md` — dodano log Fazy 0

## Powod / kontekst
Start Fazy 0 z planu MVP. Greenfield project — przedtem byl tylko PLAN.md.

**Odchylenia od planu:**
1. Plan zaklada `app/_layout.tsx` w root. Template SDK 56 trzyma routes w `src/app/_layout.tsx` (alias `@/*` -> `./src/*`). Funkcjonalnie identyczne.
2. Tailwind v3.4 zamiast v4 — nativewind@4.2.4 peer dep wymaga v3.x. Plan "NativeWind v4" zachowany (nativewind v4, tailwind v3.4).
3. Zwykle `Tabs` zamiast `NativeTabs` — NativeTabs wymaga PNG ikon per tab, polish ikon to Faza 6.

Odchylenia zaakceptowane przez usera.

## Walidacja
- typecheck (`npx tsc --noEmit`): PASS (0 bledow)
- test: n/a (Faza 0 = setup, plan nie definiuje testow)
- runtime: weryfikacja w Expo Go zaplanowana po dodaniu kluczy Supabase i commicie 9eea447
