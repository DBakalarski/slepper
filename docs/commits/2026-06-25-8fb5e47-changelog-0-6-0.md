# 8fb5e47: chore(release): changelog 0.6.0 (tryb nocny) + bump wersji

**Data:** 2026-06-25
**Branch:** main
**Faza zadania:** Faza 7 — release/ogłoszenie (krok „Utrzymanie" z roadmapy)

## Co zostało zrobione
Ogłoszenie deployu trybu nocnego, którego zabrakło przy commicie `4d28f57`:
- Wpis `v: 6` / `0.6.0` w `public/changelog.json` (4 itemy user-facing: czarny ekran + auto-dim, tap budzi bez kończenia snu, hold-to-end, oszczędność baterii).
- Sync `version` → `0.6.0` w `app.json` i `package.json`.

## Zmienione pliki
- `packages/sleeper-web/public/changelog.json` — nowy wpis v6 na górze
- `packages/sleeper-web/app.json` — version 0.5.0 → 0.6.0
- `packages/sleeper-web/package.json` — version 0.5.0 → 0.6.0

## Powód / kontekst
Baner „Co nowego" (`useChangelogUpdate` + `selectUnseen`) pokazuje tylko wpisy z `v > lastSeen`. Bez nowego wpisu deploy trybu nocnego nie wywołałby banera ani wymuszonego restartu, a Ustawienia pokazywałyby starą wersję 0.5.0. To udokumentowana pułapka z roadmapy (linia 28). Krok release'u został pominięty przy feature commicie i uzupełniony retroaktywnie.

⚠️ Pułapka cache (do walidacji lokalnej): `version` z `app.json` jest wpiekane do bundla web (`APP_MANIFEST`) i Metro cache'uje to w `$TMPDIR/metro-cache` — lokalny `expo export` bez `--clear` da bundle ze starą wersją. Vercel buduje na zimno → produkcja OK. `changelog.json` to statyczny asset (no-store fetch w runtime), nie wymaga rebuildu.

## Walidacja
- JSON: PASS (`JSON.parse` OK, top `v=6`)
- wersje spójne: PASS (changelog 0.6.0 == app.json == package.json)
- typecheck/test/build: n/a dla tej zmiany (content/config; feature walidowany w `4d28f57`)
- runtime: wymaga deployu na Vercel (push main) — baner pojawi się po wejściu na świeży bundle prod
