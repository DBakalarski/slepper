# 04622d7: fix(mvp-sleep-tracker): poprawki po review fazy 3 (cykl 1)

**Data:** 2026-05-27
**Branch:** feature/mvp-sleep-tracker
**Faza zadania:** Faza 3 — Historia + edycja (review fixes, cykl 1 z 2)

## Co zostalo zrobione

Naprawione 2 × P2 z `review-faza-3.md`:

- **P2-1 [arch]**: `src/app/(app)/session/[id].tsx` 358 LOC → 183 LOC. Wyekstrahowany `SessionEditForm` (presentational) do `features/sessions/components/SessionEditForm.tsx` (192 LOC) oraz shared `Chip` do `components/Chip.tsx` (27 LOC). `BackdatedSessionModal` zaktualizowany do uzycia shared `Chip` (lokalny `TypeChip` usuniety). Trzecie uzycie chipa (`ModeChip` w `history.tsx`) zostawione bez zmian — to byl wskaznik P3, nie P2.
- **P2-2 [correctness/tz]**: lokalny `combineDateAndTime` (uzywal `setHours` na device tz) usuniety. Nowy helper `combineDateAndTimeInAppTz` dodany do `src/lib/time.ts` — sklada YYYY-MM-DD z `datePart` w app tz z HH:mm z `timePart` w app tz i konwertuje przez `fromZonedTime`. Pattern z `parseAppTzDateTime` (utrwalony po Fazie 2).

Zaznaczono checkboxy w sekcji "Do poprawy po review fazy 3" w `mvp-sleep-tracker-zadania.md` z notatka FIX dla kazdego punktu.

## Zmienione pliki

- `sleeper-app/src/app/(app)/session/[id].tsx` — refactor: import `SessionEditForm`, usuniety `combineDateAndTime` i lokalny `TypeChip`, plik zredukowany do screen-level routing/data fetching (183 LOC)
- `sleeper-app/src/features/sessions/components/SessionEditForm.tsx` — nowy, presentational form (typ + start date/time + end date/time + notes + save/delete buttons)
- `sleeper-app/src/components/Chip.tsx` — nowy shared chip (selected/label/onPress), uzywany w session edit i BackdatedSessionModal
- `sleeper-app/src/features/sessions/components/BackdatedSessionModal.tsx` — import shared `Chip`, usuniety lokalny `TypeChip`
- `sleeper-app/src/lib/time.ts` — dodany `combineDateAndTimeInAppTz`
- `docs/active/mvp-sleep-tracker/mvp-sleep-tracker-zadania.md` — odznaczone 2 × P2, dodane notatki FIX

## Powod / kontekst

Cykl 1 napraw po `dev-docs-review` fazy 3. Severity gate przed naprawkami: ⚠️ KONTYNUUJ Z ZASTRZEZENIAMI (0 × P1, 2 × P2). P3 (7 sztuk) zostawione w backlogu zgodnie z dyrektywa pipeline. P2-1 wymagal rule-compliance (coding-rules §1 — plik > 300 LOC = refactor); P2-2 byl regresem TZ-safety wzorca utrwalonego w `learned-patterns.md` po Fazie 2.

## Walidacja

- typecheck: PASS (`npx tsc --noEmit` — 0 errors)
- lint: PASS (`npm run lint` — 0 errors, 0 warnings)
- runtime: n/a w tym cyklu (mobile-manual checklist juz w `manual-test-faza-3.md`, niezmieniony zakres)
- LOC `session/[id].tsx`: 183 (< 300, zgodne z coding-rules §1)
