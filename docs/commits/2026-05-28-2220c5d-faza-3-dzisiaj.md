# 2220c5d: feat(ui-redesign): faza 3 — dzisiaj redesign

**Data:** 2026-05-28
**Branch:** feature/ui-redesign
**Faza zadania:** ui-redesign — Faza 3 (Dzisiaj redesign)

## Co zostalo zrobione

- Nowy `sleeper-app/src/components/HomeHeader.tsx` — Avatar + greeting per godzina (Dzien dobry / Dobre popoludnie / Dobry wieczor / Dobranoc), ChevronDown visual obok imienia (single-child, no dropdown wg decyzji Fazy 0), Bell `IconButton` po prawej z dot statusu (`mock=true`).
- Nowy `sleeper-app/src/lib/useNotificationDot.ts` — mock hook `() => true` (decyzja Fazy 0, podlaczenie pod `expo-notifications` poza scope'em redesignu).
- Restyle `sleeper-app/src/components/ActiveWindowCard.tsx` — solid `bg-orange-soft` (gradient SKIP wg decyzji Fazy 0), kropka 8x8 + label "OKNO AKTYWNOSCI" uppercase tracking-wide text-orange, timer `text-6xl` z `fontVariant: ['tabular-nums']`, `ProgressBar` pod timerem (tint orange, track bialy/70), footer "Pobudka o HH:MM" (left) + `Badge variant="orange"` "Drzemka za ~Xg Ym" (right).
- Restyle `sleeper-app/src/components/TodayStatsCard.tsx` — label "DZISIAJ" text-muted, wartosc `formatDuration(totalSleepMs)` text-3xl tabular-nums + "z 13g zalecanych" text-muted, `ProgressRing` 64px z procentem `total/recommendedHours` po prawej, `ProgressBarStacked` (purple = nightSleep, orange = naps, success = longestAwake; znormalizowane do 24h), 3 mini-statystyki z kropkami w grid (Sen nocny / Drzemki "N · Xg Ym" / Aktywnosc). Reuse `computeAggregates()` 1:1, nowy opcjonalny prop `recommendedHours?` default 13.
- Restyle `sleeper-app/src/components/BigActionButton.tsx` — opcjonalny prop `sessionType?: SessionType` (default `'nap'`), `Moon` lucide ikona przed labelem dla `mode='start'` + `sessionType='night_sleep'`. Container przepiety na `flex-row items-center justify-center gap-2` + zmiana `rounded-2xl` -> `rounded-card` dla spojnosci tokenowej.
- Restyle `sleeper-app/src/components/QuickActions.tsx` — 3 biale karty z okraglym ikonowym chipem (10x10 rounded-pill): `Sun` ikona w `bg-orange-soft`, `Moon` w `bg-purple-soft`, `Plus` w `bg-cream dark:bg-dark-surface`. Helper `ActionCard` z `chipClassName` + `iconColor` (HEX, pattern z Fazy 2). Label `text-xs font-medium` pod ikona.
- `sleeper-app/src/app/(app)/index.tsx` — wymiana headera na `<HomeHeader child={activeChild} />` (fallback do "Dzisiaj" + email gdy `activeChild` null, np. brak rodziny lub przed dodaniem dziecka), `useRouter()`, "Pokaz wszystkie" link → `Pressable` z `router.push('/history')` (a11y label + role), `BigActionButton` dostaje `sessionType={activeSession?.type ?? 'nap'}`. Sekcja label "Sesje dzisiaj" zmieniona z `text-purple` na `text-text-muted` (spojnosc z reszta uppercase labelek).

## Zmienione pliki

- `sleeper-app/src/components/HomeHeader.tsx` — NEW (Avatar + greeting + Bell)
- `sleeper-app/src/lib/useNotificationDot.ts` — NEW (mock hook)
- `sleeper-app/src/components/ActiveWindowCard.tsx` — restyle (bg-orange-soft, text-6xl timer, ProgressBar, Badge footer)
- `sleeper-app/src/components/TodayStatsCard.tsx` — restyle (ProgressRing, ProgressBarStacked, mini-stats z kropkami)
- `sleeper-app/src/components/BigActionButton.tsx` — restyle (Moon dla night_sleep, sessionType prop, rounded-card)
- `sleeper-app/src/components/QuickActions.tsx` — restyle (3 karty z chipami Sun/Moon/Plus)
- `sleeper-app/src/app/(app)/index.tsx` — HomeHeader mount, router.push('/history'), sessionType thread
- `docs/active/ui-redesign/ui-redesign-zadania.md` — Faza 3 checkboxes ✅
- `docs/active/ui-redesign/ui-redesign-kontekst.md` — log progresu Fazy 3 + kluczowe decyzje

## Powod / kontekst

Faza 3 z planu `ui-redesign-plan.md`. Cel: ekran Dzisiaj wyglada zgodnie ze screenem #1 z `design.md`, BEZ zmian backendu/data flow. Decyzje Fazy 0 (`mock=true` dla Bell dot, solid `bg-orange-soft` zamiast gradient, single-child no dropdown) zaaplikowane wprost. `SessionListItem` swiadomie NIE zmieniany — w Fazie 4 (Historia) bedzie pelny rewrite (sun/moon ikona w chipie, zakres dużą czcionka, ChevronRight, prop `gapBeforeMs?`). YAGNI: nie dodawalem prop w SessionListItem dla Fazy 4 bez 2+ uzyc.

Odchylenia od planu: brak. `TodayStatsCard.recommendedHours` jako opcjonalny prop z default 13 = trzymanie zgodnosci z design.md "z 13g zalecanych"; podpiecie do `getNormForChild(birthDate).maxHours` to scope Fazy 5 (wymaga birth date threading, ktore wymaga zmiany sygnatury — odlozone wg "NIE zmieniaj data flow" w task.md).

## Walidacja

- typecheck (`npx tsc --noEmit` w `sleeper-app/`): PASS (0 bledow)
- lint (`npm run lint` w `sleeper-app/`): PASS (0 bledow)
- runtime: nie wykonano (manual on-device w Fazie 7 — checklist `manual-test-faza-2.md` + nowa checklist Fazy 3 do wygenerowania)
