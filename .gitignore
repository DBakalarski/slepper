summary.md
.DS_Store

# pnpm workspace root + per-package
node_modules/
.pnpm-store/
*.log

# Build artifacts
dist/
coverage/

# Supabase CLI cache (link ref) — root i per-package
**/supabase/.temp/

# Local env / secrets
.env
.env.local
.env.*.local

# Local Claude config
.claude/settings.local.json
.claude/tsc-cache/

# Per-package ignores are also enforced by packages/*/.gitignore

# Materialy referencyjne — copyright (Marta Stam / Kotki Dwa)
data-book/
